UPDATE tipo_ensayo SET config_calculos = '{
  "steps": [
    {
      "comment": "Cálculos para CBR - A ser implementado",
      "formula": "0",
      "inputs": {},
      "output": "results.cbr.placeholder"
    }
  ]
}' WHERE nombre_ensayo = 'CBR';
